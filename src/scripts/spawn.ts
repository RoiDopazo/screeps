export const spawnHarvester = (spawn) => {


  return Game.spawns['Spawn1'].spawnCreep( [WORK, CARRY, MOVE], 'Upgrader1' );
}

export const spawnUpgrader = (spawn) => {


  return Game.spawns['Spawn1'].spawnCreep( [WORK, CARRY, MOVE], 'Upgrader1' );
}
